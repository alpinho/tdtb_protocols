# -*- coding: utf-8 -*-
[config]

# %%
# ====================== SPECIFICATIONS FOR PC1 ========================

# For main menu
mmtxtsize = 30
# Number of main-menu items shown at once (menu scrolls if longer).
menu_scroll = 14
display_note_pos = 0, -600
devwin_size = 2400, 1800

# For instructions
# Size of text box
bsize = 2600, 1000
# Size of the sentences' letters
itxtsize = 100
# Feedback Box position
fbox_pos = 0, -120
# Reposition of y-coordinate of rectangle pairs during demonstration
rect_yrepos = -300

# For protocols (all types of sessions)
# Size of the text
txtsize = 70
task_name_size = 150

# Parameters that are constant across all files
# Fixation cross
fixcross_size = 100, 100
fixcross_thickness = 12
# Visual Stimuli
rectangle_size = 500, 1000
# Smaller rectangle for explicit-variant subdivisions (centred)
small_rectangle_size = 150, 300
circle_radius = 500
triangle_length1 = 1000
triangle_length2 = 1000
triangle_pos = 0, 50
# Screen settings for perception-protocols panel
box_size = 3000, 1300
box_pos = 0, -300
box_txtsize = 100